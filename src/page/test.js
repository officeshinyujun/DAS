function test() {

    // const fetchData = async () => {
    //     if (profileId) {
    //         try {
    //             const res = await fetch(`https://tetr.io/api/users/${profileId}`);
    //             const json = await res.json();
    //             const profileData = json.user.id;
    //             setProfile(profileData);
    //         } catch (error) {
    //             console.error("Error fetching data:", error);
    //         }
    //     }
    // };
    // <input onChange={inputId}/>
    // <button>wow</button>
}